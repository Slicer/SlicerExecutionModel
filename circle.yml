machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker pull lucasgandel/slicerexecutionmodel

test:
  override:
    - mkdir ~/SlicerExecutionModel-build
    - docker run -v ~/SlicerExecutionModel:/usr/src/SlicerExecutionModel -v ~/SlicerExecutionModel-build:/usr/src/SlicerExecutionModel-build lucasgandel/slicerexecutionmodel /usr/src/SlicerExecutionModel/test/Docker/test.sh

